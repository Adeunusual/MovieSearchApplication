<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyMovieSearchApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/netflix_red</item>
        <item name="colorPrimaryVariant">@color/netflix_red</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- FIXED background attributes -->
        <item name="android:colorBackground">@color/black</item>
        <item name="background">@color/black</item>

        <item name="colorOnBackground">@color/white</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.MyMovieSearchApplication" parent="Base.Theme.MyMovieSearchApplication" />
</resources>
